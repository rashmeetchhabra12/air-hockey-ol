name = "tick-spike"
main = "src/index.ts"
compatibility_date = "2025-09-01"

[[durable_objects.bindings]]
name = "ROOM"
class_name = "TickRoom"

# SQLite-backed Durable Objects are the class available on the free plan.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["TickRoom"]
