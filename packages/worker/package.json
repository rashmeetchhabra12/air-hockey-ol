{
  "name": "@ah/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Durable Object adapter: hosts one GameRoom per match.",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@ah/protocol": "*",
    "@ah/server": "*",
    "@ah/sim": "*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "wrangler": "^3.99.0"
  }
}
